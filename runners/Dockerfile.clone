# Runner image for the clone phase of repo analysis.
# Pre-built so there is zero pull/install time when the backend spawns this container.
#
# Extends alpine/git which already ships git. We pin ca-certificates explicitly
# so HTTPS clones work against any host without runtime network calls.
FROM alpine/git:latest

RUN apk add --no-cache ca-certificates

WORKDIR /workspace
