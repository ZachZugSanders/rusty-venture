[project]
name = "rusty-venture"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-playwright>=0.5", "playwright>=1.44"]

[tool.setuptools]
packages = []

[tool.pytest.ini_options]
testpaths = ["tests"]
